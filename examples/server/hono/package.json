{
  "name": "@armory/example-hono-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot run server.ts",
    "start": "bun run server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@armory/middleware": "workspace:*",
    "@armory/facilitator": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
