{
  "name": "@armory/example-express-server",
  "version": "0.1.0",
  "type": "module",
  "description": "Express server example with X-402 payment middleware",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "start": "bun run src/server.ts",
    "client": "bun run src/client-example.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@armory/core": "workspace:*",
    "@armory/facilitator": "workspace:*",
    "@armory/middleware": "workspace:*",
    "express": "^4.18.2",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.0.0",
    "bun-types": "^1.0.0"
  }
}
