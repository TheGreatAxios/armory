{
  "name": "@armory/facilitator-standard",
  "version": "0.1.0",
  "type": "module",
  "description": "X-402 Facilitator - Standard implementation with in-memory queue and nonce tracker",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --hot run server.ts"
  },
  "dependencies": {
    "@armory/core": "workspace:*",
    "@armory/facilitator": "workspace:*",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
