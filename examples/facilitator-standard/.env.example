# ============================================================================
# X-402 Facilitator Configuration
# ============================================================================

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Private key for signing settlement transactions
# IMPORTANT: Never commit this file to version control
# Leave empty to run in "simulated" mode (no actual on-chain settlement)
PRIVATE_KEY=

# RPC URLs for supported networks
# Override with your own RPC endpoints for better reliability
RPC_ETHEREUM=https://eth.llamarpc.com
RPC_BASE=https://mainnet.base.org
RPC_OPTIMISM=https://mainnet.optimism.io
RPC_ARBITRUM=https://arb1.arbitrum.io/rpc
RPC_POLYGON=https://polygon-rpc.com

# Queue Configuration
# Queue worker poll interval in milliseconds (default: 1000)
QUEUE_POLL_INTERVAL=1000

# Maximum retry attempts for failed settlements (default: 3)
QUEUE_MAX_RETRIES=3

# Nonce Tracker Configuration
# TTL for nonce entries in milliseconds (default: undefined = no expiration)
# Recommended: 86400000 (24 hours) for production
NONCE_TTL=86400000
